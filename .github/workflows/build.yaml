name: build

on:
  push:
    branches:
      - main

jobs:
  build_package:
    uses: bitcompat/base/.github/workflows/build.yaml@main
    with:
      name: main
      build-args: |
        RUN_TESTS=1
      image_tag: |
        latest
        0.15-20220414-bullseye-r1 # renovate: datasource=github-tags depName=json-c/json-c
        0.15-bullseye-r1 # renovate: datasource=github-tags depName=json-c/json-c versioning=^(?<major>\d+)\.(?<minor>\d+)(?:-.*)?$
        0.15-bullseye # renovate: datasource=github-tags depName=json-c/json-c versioning=^(?<major>\d+)\.(?<minor>\d+)(?:-.*)?$
        0.15 # renovate: datasource=github-tags depName=json-c/json-c versioning=^(?<major>\d+)\.(?<minor>\d+)(?:-.*)?$
    secrets: inherit
